# API base URL.
# Unified server (localhost): use a relative path.
# Separate Vercel frontend + EC2 backend: set to the full backend URL.
# NEXT_PUBLIC_API_URL="https://your-backend-domain.com/api"
NEXT_PUBLIC_API_URL="/api"
